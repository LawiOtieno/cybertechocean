export interface Service {
  id: string;
  name: string;
  category: string;
  description: string;
  price: string;
  image: string;
  details: string;
}

export interface SocialLink {
  platform: string;
  url: string;
  icon: any;
}
